import type { LocationTuple } from '@/interfaces';

export const DEFAULT_CENTER: LocationTuple = [50.4501, 30.5234];
export const DEFAULT_ZOOM: number = 8;
export const DRAWER_WIDTH: number = window.innerWidth > 600 ? 316 : 0;
